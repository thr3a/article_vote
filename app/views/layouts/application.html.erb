<!DOCTYPE html>
<html>
  <head>
    <title><%= Rails.application.config.title %></title>
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <header>
    <nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
      <a class="navbar-brand" href="/"><%= Rails.application.config.title %></a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbar-header" aria-controls="navbar-header" aria-expanded="false">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbar-header">
        <ul class="navbar-nav mr-auto">
          <li class="nav-item active">
            <%= link_to "投票一覧", votes_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "設定", votes_path, class: "nav-link" %>
          </li>
          <li class="nav-item">
            <%= link_to "投票フォーム設置方法", start_votes_path, class: "nav-link" %>
          </li>
        </ul>
        <ul class="nav navbar-nav navbar-right">
        <% if signed_in? %>
          <li class="active"><%= link_to "ログアウト", "/logout", class: "nav-link"%></li>
        <% else %>
          <li class="active"><%= link_to "ログイン", "/auth/twitter", class: "nav-link"%></li>
        <% end %>
        </ul>
      </div>
    </nav>
  </header>
    <div class="container">
      <%= yield %>
    </div>
  </body>
</html>
